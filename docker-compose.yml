
services:
  # cp-nifty-analyzer:
  #   build: .
  #   container_name: cp-nifty-analyzer
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NODE_ENV=production
  #     - DATABASE_URL=postgresql://postgres:password@cp-nifty-postgres:5432/postgres
  #   depends_on:
  #     - postgres
  #   networks:
  #     - cp-nifty-network




volumes:
  postgres_data:

networks:
  cp-nifty-network:
    driver: bridge